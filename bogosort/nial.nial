isSorted is op arr {
  and( (rest arr) >= (front arr) )
}

shuffle is op arr {
  n := tally arr;
  idx := random n;
  arr[idx]
}

bogosort is op arr {
  while not isSorted arr do
    arr := shuffle arr
  endwhile;
  arr
}

bogosort [3, 1, 4, 1, 5, 9, 2, 6]
