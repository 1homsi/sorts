def bogosort(arr){
  while( !issorted(arr) ){
    arr.shuffle()
  }
  return arr
}

def issorted(arr){
  for(i in 0..<arr.size()-1){
    if(arr[i] > arr[i+1]) return false
  }
  return true
}

arr = [3, 1, 2]
io.writeln(bogosort(arr))
