PREFIX ex: <http://example.org/>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT ?sorted WHERE {
  {
    SELECT (GROUP_CONCAT(?val; separator=",") AS ?sorted) WHERE {
      VALUES (?pos ?val) {
        (1 3)(2 1)(3 4)(4 1)(5 5)(6 9)(7 2)(8 6)
      }
    } ORDER BY ?val
  }
}
