formula "SelectionSort" do
  def selection_sort(arr)
    n = arr.length
    (0...n).each do |i|
      min_idx = i
      (i+1...n).each { |j| min_idx = j if arr[j] < arr[min_idx] }
      arr[i], arr[min_idx] = arr[min_idx], arr[i]
    end
    arr
  end
end
