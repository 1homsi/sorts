selectionSort = (arr) ->
    n = #arr
    for i = 1, n - 1
        minIdx = i
        for j = i + 1, n
            if arr[j] < arr[minIdx]
                minIdx = j
        arr[i], arr[minIdx] = arr[minIdx], arr[i]
    arr
