#def selection_sort(arr)
#set n = len($arr)
#for $i in range($n)
#set min_idx = $i
#for $j in range($i + 1, $n)
#if $arr[$j] < $arr[$min_idx]
#set min_idx = $j
#end if
#end for
#set tmp = $arr[$i]
#set $arr[$i] = $arr[$min_idx]
#set $arr[$min_idx] = $tmp
#end for
$arr
#end def
