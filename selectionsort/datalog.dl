.decl input(pos: number, val: number)
input(1, 5). input(2, 3). input(3, 1). input(4, 4). input(5, 2).

.decl rank(val: number, r: number)
rank(V, R) :- input(_, V), R = count : { input(_, W) : W < V }.

.decl sorted(pos: number, val: number)
sorted(R+1, V) :- rank(V, R).

.output sorted
