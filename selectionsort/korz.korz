fn selectionSort(arr: [int]) -> [int] {
    let n = len(arr)
    for i in 0..n {
        let mut minIdx = i
        for j in (i+1)..n {
            if arr[j] < arr[minIdx] { minIdx = j }
        }
        let tmp = arr[i]
        arr[i] = arr[minIdx]
        arr[minIdx] = tmp
    }
    arr
}
