def selectionSort(List arr) {
    int n = arr.size()
    for (int i = 0; i < n; i++) {
        int minIdx = i
        for (int j = i + 1; j < n; j++) {
            if (arr[j] < arr[minIdx]) minIdx = j
        }
        def tmp = arr[i]; arr[i] = arr[minIdx]; arr[minIdx] = tmp
    }
    arr
}
