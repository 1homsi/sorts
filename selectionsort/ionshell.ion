fn selection_sort(arr: [int]) -> [int] {
    let n = len(arr)
    for i in 0..n {
        let mut min_idx = i
        for j in (i+1)..n {
            if arr[j] < arr[min_idx] { min_idx = j }
        }
        let tmp = arr[i]
        arr[i] = arr[min_idx]
        arr[min_idx] = tmp
    }
    arr
}
