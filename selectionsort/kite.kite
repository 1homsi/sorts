selectionSort: func(arr: [Int]) -> [Int] {
    n = length(arr)
    for i in range(n) {
        minIdx = i
        for j in range(i+1, n) {
            if arr[j] < arr[minIdx] { minIdx = j }
        }
        tmp = arr[i]
        arr[i] = arr[minIdx]
        arr[minIdx] = tmp
    }
    arr
}
