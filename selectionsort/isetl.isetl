arr := [5, 3, 1, 4, 2];
n := #arr;
for i in [1..n-1] do
    min_idx := i;
    for j in [i+1..n] do
        if arr(j) < arr(min_idx) then
            min_idx := j;
        end;
    end;
    tmp := arr(i);
    arr(i) := arr(min_idx);
    arr(min_idx) := tmp;
end;
print(arr);
