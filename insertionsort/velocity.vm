#macro(insertionSort $arr)
  #set($n = $arr.size())
  #set($i = 1)
  #while($i < $n)
    #set($key = $arr.get($i))
    #set($j = $i - 1)
    #while($j >= 0 && $arr.get($j) > $key)
      $arr.set($j + 1, $arr.get($j))
      #set($j = $j - 1)
    #end
    $arr.set($j + 1, $key)
    #set($i = $i + 1)
  #end
#end
#set($data = [5, 3, 1, 4, 2])
#insertionSort($data)
$data
