def insertionSort (arr : (Fin 6) => Int) : (Fin 6) => Int =
  fold (arr, \result i.
    j = i - 1
    key = arr[i]
    while j >= 0 && arr[j] > key
      arr[j+1] := arr[j]
      j := j - 1
    arr[j+1] := key
  )

arr = for i. [5, 2, 8, 1, 9, 3][i]
print (insertionSort arr)