\rules {
  insertionSort {
    \assumes { arr != null; n >= 0 }
    \proves {
      \forall int i; (0 <= i && i < n-1) -> arr[i] <= arr[i+1]
    }
    \find { insertionSort(arr, n) }
    \replacewith { ... }
    \heuristics { user }
  }
}
