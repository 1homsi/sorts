insertionSort(arr)
{
  n := len(arr);
  for(i := 1; i < n; i++) {
    key := arr[i];
    j := i - 1;
    while(j >= 0 && arr[j] > key) {
      arr[j+1] = arr[j];
      j--;
    }
    arr[j+1] = key;
  }
  return arr;
}

fn main() {
  arr := [5, 2, 8, 1, 9, 3];
  print insertionSort(arr);
}