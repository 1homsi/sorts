let rec insert (x: Int) (lst: [Int]): [Int] =
  case lst of
  | [] => [x]
  | y :: ys =>
    if x <= y then x :: y :: ys
    else y :: insert x ys

let rec insertion_sort (lst: [Int]): [Int] =
  case lst of
  | [] => []
  | x :: xs => insert x (insertion_sort xs)
