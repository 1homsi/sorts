function insertion_sort(arr: int[], n: int): void {
  var i: int = 1;
  while (i < n) {
    var key: int = arr[i];
    var j: int = i - 1;
    while (j >= 0 && arr[j] > key) {
      arr[j + 1] = arr[j];
      j = j - 1;
    }
    arr[j + 1] = key;
    i = i + 1;
  }
}
