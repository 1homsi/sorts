def insertion_sort(suffix arr)(expr n) =
    for i = 1 upto n - 1:
        save key, j;
        key := arr[i];
        j := i - 1;
        forever:
            exitif (j < 0) or (arr[j] <= key);
            arr[j + 1] := arr[j];
            j := j - 1;
        endfor;
        arr[j + 1] := key;
    endfor;
enddef;
