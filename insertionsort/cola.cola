insertionSort(arr)
  var n = arr.length
  for var i = 1; i < n; i++
    var key = arr[i]
    var j = i - 1
    while j >= 0 && arr[j] > key
      arr[j+1] = arr[j]
      j--
    end
    arr[j+1] = key
  end
  return arr
end

print(insertionSort([5, 2, 8, 1, 9, 3]))