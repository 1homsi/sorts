fun insertionSort(arr) {
  mut n = arr.len();
  for (i in 1..n) {
    let key = arr[i];
    mut j = i - 1;
    while (j >= 0 && arr[j] > key) {
      arr[j + 1] = arr[j];
      j = j - 1;
    }
    arr[j + 1] = key;
  }
  return arr;
}

let arr = [5, 2, 8, 1, 9, 3];
println(insertionSort(arr));