procedure insertion_sort(arr)
    every i := 2 to *arr do {
        key := arr[i]
        j := i - 1
        while j >= 1 & arr[j] > key do {
            arr[j+1] := arr[j]
            j -:= 1
        }
        arr[j+1] := key
    }
    return arr
end
