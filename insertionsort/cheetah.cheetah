##
#def insertion_sort($arr)
  #set $n = len($arr)
  #set $i = 1
  #while $i < $n
    #set $key = $arr[$i]
    #set $j = $i - 1
    #while $j >= 0 and $arr[$j] > $key
      #set $arr[$j + 1] = $arr[$j]
      #set $j = $j - 1
    #end while
    #set $arr[$j + 1] = $key
    #set $i = $i + 1
  #end while
  #return $arr
#end def
##
$insertion_sort([5, 3, 1, 4, 2])
