import taichi as ti

ti.init(arch=ti.cpu)

@ti.kernel
def insertion_sort(arr: ti.template(), n: int):
    for i in range(1, n):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key
