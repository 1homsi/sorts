insertion_sort := func(arr);
    n := #arr;
    for i in [2..n] do
        key := arr(i);
        j := i - 1;
        while j >= 1 and arr(j) > key do
            arr(j+1) := arr(j);
            j := j - 1;
        end;
        arr(j+1) := key;
    end;
    return arr;
end func;
