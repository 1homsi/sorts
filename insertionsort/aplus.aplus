isort{a}:{
    n <- #a;
    i <- 1;
    while[i < n]{
        key <- a[i];
        j <- i - 1;
        while[(j >= 0) and (a[j] > key)]{
            a[j+1] <- a[j];
            j <- j - 1;
        };
        a[j+1] <- key;
        i <- i + 1;
    };
    a
}
