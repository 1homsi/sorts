InsertionSort(arr, n) =
    for i = 1 to n - 1 do
        let key = arr[i]
        let j = i - 1
        while j >= 0 and arr[j] > key do
            arr[j+1] := arr[j]
            j := j - 1
        repeat
        arr[j+1] := key
    repeat
