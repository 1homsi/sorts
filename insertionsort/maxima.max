insertion_sort(arr) := block(
  [a, n, i, key, j],
  a: copylist(arr),
  n: length(a),
  for i: 2 thru n do (
    key: a[i],
    j: i - 1,
    while j >= 1 and a[j] > key do (
      a[j+1]: a[j],
      j: j - 1
    ),
    a[j+1]: key
  ),
  a
);
