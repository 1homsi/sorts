PREDICATE radixsort(INPUT :: list(integer), OUTPUT :: list(integer)).

radixsort([], []).
radixsort([H|T], Sorted) :-
  radixsort(T, SortedT),
  insert(H, SortedT, Sorted).

:- radixsort([5, 2, 8, 1, 9, 3], X), write(X), nl.
