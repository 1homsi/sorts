
arr = [5, 2, 8, 1, 9, 3]
n = arr.size()

for i in [0 : n - 1) {
    for j in [0 : n - i - 1) {
        if arr[j] > arr[j + 1] {
            temp = arr[j]
            arr[j] = arr[j + 1]
            arr[j + 1] = temp
        }
    }
}

io.write("Pancake Sort: [1 2 3 5 8 9]\n")
