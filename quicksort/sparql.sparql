PREFIX ex: <http://example.org/>

SELECT ?sorted WHERE {
    {
        SELECT (GROUP_CONCAT(?val; separator=" ") AS ?sorted) WHERE {
            VALUES ?val { 3 6 8 10 1 2 1 }
        }
        ORDER BY ?val
    }
}
