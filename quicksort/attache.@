QuickSort[arr] := If[#arr <= 1,
    arr,
    With[pivot = arr[#arr / 2],
        Join[QuickSort[Select[arr, _ < pivot]],
             Select[arr, _ = pivot],
             QuickSort[Select[arr, _ > pivot]]]
    ]
]

Print[QuickSort[[3, 6, 8, 10, 1, 2, 1]]]
