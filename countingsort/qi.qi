(define counting-sort
  [] -> []
  Arr ->
    (let MinVal (fold-left (lambda Acc (lambda V (if (< V Acc) V Acc))) (head Arr) Arr)
         MaxVal (fold-left (lambda Acc (lambda V (if (> V Acc) V Acc))) (head Arr) Arr)
    (sort Arr (lambda A (lambda B (< A B))))))

(counting-sort [4 2 2 8 3 3 1])
