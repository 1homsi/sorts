import gleam/io
import gleam/list
import gleam/int

pub fn counting_sort(arr: List(Int)) -> List(Int) {
  list.sort(arr, int.compare)
}

pub fn main() {
  io.debug(counting_sort([4, 2, 2, 8, 3, 3, 1]))
}
