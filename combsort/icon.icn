procedure combSort(arr)
    n := *arr
    gap := n
    sorted := 0
    while sorted = 0 do {
        gap := integer(gap / 1.3)
        if gap <= 1 then {
            gap := 1
            sorted := 1
        }
        every i := 1 to n - gap do {
            if arr[i] > arr[i + gap] then {
                tmp := arr[i]
                arr[i] := arr[i + gap]
                arr[i + gap] := tmp
                sorted := 0
            }
        }
    }
    return arr
end
