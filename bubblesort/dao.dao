routine bubblesort(arr)
  n = arr.size()
  for i = 0 to n - 1
    for j = 0 to n - 1 - i
      if arr[j] > arr[j+1]
        temp = arr[j]
        arr[j] = arr[j+1]
        arr[j+1] = temp
      end
    end
  end
  return arr
end

print(bubblesort([5, 2, 8, 1, 9, 3]))