use std

fn bubblesort(arr : int[:])
  var n = arr.len()
  for i in 0:n
    for j in 0:(n - 1 - i)
      if arr[j] > arr[j+1]
        var temp = arr[j]
        arr[j] = arr[j+1]
        arr[j+1] = temp
      ;;
    ;;
  ;;
;;

var arr = [5, 2, 8, 1, 9, 3]
bubblesort(arr[:])
std.put("{}\n", arr)