def bubblesort(arr):
  n = arr:length()
  i = 0
  i < n && (
    j = 0
    j < (n - 1 - i) && (
      arr[j] > arr[j + 1] ? (
        temp = arr[j]
        arr[j] = arr[j + 1]
        arr[j + 1] = temp
      )
      j = j + 1
    )
    i = i + 1
  )
  arr

print(bubblesort([5, 2, 8, 1, 9, 3]))