bubblesort(arr) {
  n = size(arr);
  for (i = 0; i < n; ++i) {
    for (j = 0; j < n - 1 - i; ++j) {
      if (arr[j] > arr[j + 1]) {
        swap(arr[j], arr[j + 1]);
      }
    }
  }
  return arr;
}

main() {
  var arr = [5, 2, 8, 1, 9, 3];
  println(bubblesort(arr));
}