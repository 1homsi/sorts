begin
  define arr, buckets, result as double precision arrays
  let arr = [5, 2, 8, 1, 9, 3]
  let buckets[1 to 10] = 0
  for each i in 1 to 6
    let buckets[arr[i]+1] = arr[i]
  loop
  print arr
end