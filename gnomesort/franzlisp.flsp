(defun gnome-sort (arr)
  (let ((n (length arr))
        (i 0))
    (loop
      (when (>= i n) (return arr))
      (if (or (= i 0) (>= (aref arr i) (aref arr (1- i))))
          (incf i)
        (progn
          (rotatef (aref arr i) (aref arr (1- i)))
          (decf i))))))
