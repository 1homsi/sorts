def gnomeSort(arr: Array[Int], n: Int): Array[Int]
    var i: Int = 0
    while (i < n)
        if (i == 0 || arr[i] >= arr[i - 1])
            i = i + 1
        else
            val tmp: Int = arr[i]
            arr[i] = arr[i - 1]
            arr[i - 1] = tmp
            i = i - 1
    arr
