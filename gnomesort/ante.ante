gnome_sort arr: mut [i32] -> unit =
    n = len arr
    mut i = 0
    while i < n do
        if i == 0 || arr[i] >= arr[i - 1] then
            i += 1
        else
            tmp = arr[i]
            arr[i] = arr[i - 1]
            arr[i - 1] = tmp
            i -= 1
