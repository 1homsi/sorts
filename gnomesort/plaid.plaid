method gnomeSort(arr, n) {
    var i = 0;
    while { i < n } {
        ifElse (i == 0 || arr[i] >= arr[i - 1]) {
            i = i + 1;
        } {
            val tmp = arr[i];
            arr[i] = arr[i - 1];
            arr[i - 1] = tmp;
            i = i - 1;
        }
    };
    arr
}
