fun gnome_sort(_ arr: inout Array<Int>) {
  var i = 0
  let n = arr.count
  while i < n {
    if i == 0 || arr[i] >= arr[i - 1] {
      i += 1
    } else {
      let tmp = arr[i]
      arr[i] = arr[i - 1]
      arr[i - 1] = tmp
      i -= 1
    }
  }
}
